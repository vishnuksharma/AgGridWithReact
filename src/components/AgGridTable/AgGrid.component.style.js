const styles = ( (theme) => ({
  agGridContainer: {
    height: '70vh',
    width: '100%',
  },
  '& .ag-floating-top .ag-row': {
    backgroundColor: 'black',
  },
  '& .ag-floating-bottom .ag-row': {
    backgroundColor: 'black',
  }
}));

export default styles;